import React, { type FC } from "react";

import { Layout } from "../components/Layout";

export const NotFound: FC = () => {
  return (
    <Layout>This is empty page. Press &quot;backwards&quot; in browser</Layout>
  );
};
